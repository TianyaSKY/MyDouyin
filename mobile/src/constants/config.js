// API base URL - change this to your backend server IP
// For Android emulator, use 10.0.2.2 to access host machine's localhost
// For physical device, use your machine's LAN IP (e.g. 192.168.x.x)
export const API_BASE_URL = 'http://10.0.2.2:8081';

// Media base URL (same as API for this project)
export const MEDIA_BASE_URL = API_BASE_URL;

// App info
export const APP_NAME = 'Douyin';
export const APP_VERSION = '1.0.0';
